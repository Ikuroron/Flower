$(function () {

    // bodyを遅延で表示
    var bodyIn= {
        delay: 000,
        distance: '500px',
        origin: 'top',
        };
        ScrollReveal().reveal('body', bodyIn);

        // $("body").fadeIn(3000);
})